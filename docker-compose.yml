version: "3.9"

services:
  db:
    # Image officielle pgRouting (PostgreSQL 16 + PostGIS 3.5 + pgRouting 3.8).
    # NB: ne pas utiliser postgis/postgis ici — elle n'inclut pas pgRouting
    # (init-db/01-init.sql échouerait sur CREATE EXTENSION pgrouting).
    image: pgrouting/pgrouting:16-3.5-3.8
    container_name: obstacles-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-routing_db}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: obstacles-backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - ./backend/.env
    environment:
      POSTGRES_SERVER: db
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-routing_db}
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE: ${VITE_API_BASE:-http://localhost:8000}
    container_name: obstacles-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "5173:80"
    # For dev mode alternative, override command:
    # command: npm run dev -- --host 0.0.0.0 --port 5173

volumes:
  pgdata:
